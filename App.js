import React from 'react';
import { 
  StyleSheet, Text, View, Image, SafeAreaView, 
  Button, Alert, ImageBackground , StatusBar, Platform, Dimensions 
} from 'react-native';

export default function App() {
  // console.log(useDimensions)
  return (
    <SafeAreaView>
      
    </SafeAreaView>
  );
}